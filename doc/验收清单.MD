# 签字大师 1.0 验收清单

日期：2026-07-14

## 自动化通过

- [x] 文件、模板、签名、我的四个主页面均开启微信好友分享和朋友圈分享，并使用页面级宣传标题与统一品牌封面。
- [x] “我的”页面不增加推荐宣传入口；好友和朋友圈分享统一使用四个主页面右上角的微信原生菜单。
- [x] “我的签名”预览右上角可通过 `wx.showShareImageMenu` 分享可预览 PNG；仅有轨迹时会先生成图片。
- [x] 文件首页空状态使用完整实心品牌图标，不再出现轮廓局部未填充。
- [x] 首页 Logo 双光环与空文件入口均保持常驻动效，非首次用户也不会退化成静态图标。
- [x] 四个主页面直接注册 `onShareAppMessage` 和 `onShareTimeline`，微信卡片使用宣传标题和品牌封面，不回退为页面名称与截图。

- [x] 调整页“我的签字”弹框不使用原生 Canvas 作为选择项预览，点击签字轨迹或名称均可应用签字。
- [x] 弹框签字使用微信 `touchend + tap` 双通道并去重，单次点击只新增一个图层；微信开发者工具实际交互验证通过。
- [x] 容量管理页根容器占满视口宽度，内容左右边距统一为 18px，不叠加 `PageShell` 的重复边距。

- [x] 备份按钮先显示任务状态，同一时间只执行一个备份任务；重复点击不会并发生成。
- [x] 备份固定覆盖 `signMaster-backup.signmaster`，目录中仅保留一个备份；正文 gzip 压缩后再加密。
- [x] 新增容量管理独立页，展示使用量、剩余量、分类明细和键值存储容量，支持一键清理可再生成数据。
- [x] 主要文件写入路径在写入前检查剩余空间，空间不足显示覆盖旧数据或前往清理，不直接透出微信原生配额错误。
- [x] 跟随系统主题读取真实系统主题并监听变化；微信原生自定义 TabBar 与 H5 底栏支持深色模式。
- [x] “关于”使用独立分包页面；设置页重复点击导航不会造成返回后页面失效。
- [x] 微信生产流程将自动签字位识别标记为实验性并暂时跳过，选取文件后直接进入手写，不虚报识别结果。
- [x] 调整页底部“保存”已替换为“我的签字”，签字库面板位于文档预览区域，选中后放到默认位置供调整。
- [x] 扫描器原生控制项位于 `camera` 外层覆盖层并编译为 `catchtap`，支持取消、手动快门、自动开关、重拍上一页和完成。
- [x] 连续扫描使用真实文档四边形透视裁切，左下角显示已扫描缩略图和页数，完成后生成多页 PDF。

- [x] 实验性 H5 识别签字位使用 `touchend + tap` 双通道，点击后可进入手写或弹出已有签名选择，重复事件不会执行两次。
- [x] 识别框从横线起点左对齐，宽度最多为文档宽度 32%，靠近右边缘时按剩余宽度收窄。
- [x] 除家长回执语义规则外，支持不同位置的“文字标签 + 冒号/分隔符 + 横线”通用文字表单识别。
- [x] 无标签图片边框、左侧装饰线不会被通用表单规则误识别为签字位。
- [x] 图片型文件默认 JPEG，格式顺序为 JPEG、PDF、PNG；PDF/多页 PDF 保持 PDF 导出。

- [x] 首页仅同步读取轻量文件索引，完整签署工程独立持久化并在编辑时按需加载；旧内嵌工程只迁移一次。
- [x] 用户身份、签署 Store 和模板关联签名库不阻塞首屏；微信生产包启用组件按需注入和分包优化。
- [x] 已签署文件生成最大边 360px、JPEG 质量 0.58 的独立缩略图，文件卡不读取原始大图或完整工程。
- [x] 连续三轮横屏手写返回调整页后，文档重新按竖屏编辑区测量并保持居中，不沿用横屏宽度。
- [x] 本地实验性识别器对用户提供的 3558x4965 回执图识别三个签字位；100 组亮度、噪声、平移扰动通过 100/100，微信端不自动启用。
- [x] 本地识别使用 640px 像素预算和源图快速路径，页面分析硬超时为 2.8 秒。

- [x] 微信聊天文件使用 `chooseMessageFile({ type: 'all' })`，支持 PDF 和常用图片格式。
- [x] 微信文件选择器取消或直接返回时，不显示读取 Loading，不进入识别流程。
- [x] 相册选择有效图片后进入分析流程；空结果按取消处理。
- [x] H5 实验性分析最长 3 秒并可跳过；微信生产流程直接跳过分析，不展示等待页。
- [x] 微信端跳过识别时不创建虚假识别位、不显示识别数量，直接进入手写。
- [x] 用户提供的 JPG/PNG 家长回执识别家长意见、幼儿姓名、家长签字 3 个签字位。
- [x] 选择与定位页无离散缩放组件，仅保留手势缩放；底部操作完整可见。
- [x] 选择与定位页长按文档可选择应用已有签名或手写签名，签字位按当前缩放后的长按位置创建，移动和双指缩放不会误触。
- [x] 横屏手写原生标题显示当前签字位，左侧“我的签字”为可开关状态，右侧工具栏无返回按钮。
- [x] 从签名库新增签名时仅显示原生标题，不显示“我的签字”，画布占用左侧安全区后的全部可用空间。
- [x] 文件签署的“我的签字”面板约占页面 60% 宽，无额外关闭减号，再次点击状态按钮可关闭。
- [x] 粗笔轨迹连续，橡皮、清除、撤销、重做可用，签字生成透明持久 PNG。
- [x] 调整页六个工具固定六列无横向滚动，透明度区不被保存和完成按钮挤压。
- [x] “继续签字”可进入下一个未签位置；没有剩余识别位时可创建手动位置。
- [x] 保存签名进入独立页面，默认全选，可取消全选、重命名、预览并保存返回。
- [x] 签名预览仅保留一个居中“关闭预览”按钮，无重复减号操作。
- [x] 保存模板进入独立页面，显示文档、签字位和对应签名，可修改名称并保存返回。
- [x] 保存模板内嵌对应签名快照；点击签名可预览和改名，应用模板时选取新文件后直接进入调整页。
- [x] 应用模板不重新分析新文件，不覆盖模板保存的最终签字位。
- [x] 保存模板使用调整后的最终位置、尺寸、旋转和透明度；多色签字位可点击联动高亮。
- [x] 模板、签名、已签署文件支持持久化、容量上限、重命名和确认删除。
- [x] 我的签名缩略区点击只打开签名预览，名称区点击才打开默认设置和重命名菜单。
- [x] 保存模板同步保存关联签名并记录关联 ID；返回导出页后显示禁用的“签名已保存”。
- [x] 删除模板前提示保留或删除关联签名，共享签名不会被误删。
- [x] 模板卡不使用已签文件缩略图，统一显示默认纸张图和高对比签字位，签字位不进入名称区。
- [x] 模板卡与文件卡同为 184px，删除减号使用相同位置参数。
- [x] 模板卡使用自身原生 `view` 根节点，微信小程序样式隔离下仍完整显示默认纸张图、签字位和名称区。
- [x] 应用模板完成签署后，导出页隐藏保存模板和保存签名入口。
- [x] 模板删除减号以卡片右上边框交点为定位基准。
- [x] PDF、PNG、JPEG 按需生成；分享按当前格式生成，不复用旧格式文件。
- [x] JPEG/PNG 使用微信图片分享接口，聊天中按可预览图片发送；PDF 使用文件分享接口。
- [x] 导出名采用 `原文件名_已签署_YYYYMMDD-HHmmss.扩展名`，完整名称只有一个句点。
- [x] 微信 Storage 单项超限时改用用户目录持久化，导出成功后返回不会错误显示导出失败。
- [x] JPEG/PNG 画布成品先进入可写导出目录再写防伪证据，不直接覆盖只读 `wxfile://tmp`，相册导出不出现 permission denied。
- [x] 容量管理显示受管临时文件占用，支持独立清理未完成导入、扫描裁切及处理中间文件。
- [x] 选择与定位和扫描流程主动放弃时自动清理本次临时文件；异常退出遗留项可由容量管理清理。
- [x] 已签署文件、模板、签名上限分别为 8、10、20，达到上限提示滚动覆盖或手动清理。
- [x] 图片 PDF 使用本地 JPEG-PDF 生成器，不出现 `SOI not found in jpeg`。
- [x] 扫描相机仅保留取景边框和手动拍照按钮，不自动拍摄，不显示自动扫描、重拍或相机页完成等重复控件。
- [x] 手动拍照后识别文档边沿并自动透视裁切，处理完成进入扫描预览；可继续扫描添加下一页。
- [x] 拍照成功后立即进入扫描预览，裁切在单任务后台队列执行；上一页裁切时可继续拍摄下一页。
- [x] 扫描页按真实阶段显示每页裁切进度，失败页可单独重试；完成扫描会等待队列收尾，不导出未裁切页面。
- [x] 扫描路径使用 900px 像素预算、关闭签字位分析并复用边沿结果；透视处理分片让出主线程，裁切期间页面控件保持响应。
- [x] 多页扫描预览支持删除页面和按住手柄上下拖动排序，完成后按当前顺序生成多页 PDF。
- [x] 扫描预览支持点击任意页缩略图全屏预览，多页可左右切换。
- [x] 微信扫描完成使用轻量 JPEG-PDF 生成器，不依赖通用签署导出器，不出现 `exportSignedPdf` 未定义错误。
- [x] 扫描 JPEG-PDF 生成和保存直接编译进扫描页，不再运行时调用 `scanPdf.js` 或 `pdfFileSaver.js`。
- [x] 自动裁边优先识别高亮低色差 A4 类纸张主体并恢复透视四角，不可靠时回退完整照片。
- [x] 扫描底部两个按钮填满操作区、文字图标居中、高度统一，并覆盖微信原生按钮默认外边距。
- [x] 单图片导出按 JPEG、PDF、PNG 排列并默认选中首项 JPEG；原 PDF 和多页扫描保持 PDF。
- [x] 纯前端本地用户 ID、防伪签章、水印和鉴别流程不依赖云函数或 OpenID。
- [x] 签字鉴别支持上传已签署 PDF/JPEG/PNG，报告固定先判定 signMaster 系统来源，再判定当前用户签字归属。
- [x] 新导出 PDF、JPEG、PNG 均嵌入 `SMV2` 防伪载荷，载荷包含正文 SHA-256、系统 HMAC、用户摘要、签字签章、页码和最终位置。
- [x] 鉴别时移除证据容器并恢复原正文重新计算摘要，文件正文修改或载荷移植不能通过系统来源鉴定。
- [x] 图片同时嵌入系统来源 DCT 与用户 DCT 水印，两个水印使用独立系数并可分别提取。
- [x] 鉴定报告列出报告编号、两阶段状态、签发/鉴定时间和逐个签字证据；图片中叠加提取到的签字范围。
- [x] 系统来源未通过时，用户归属显示“不予认定”，不会仅凭相似字迹给出本人结论。
- [x] 文件鉴定完成后仍可执行现场复签；本机签名档案完整性检查也可独立使用。
- [x] 四个 Tab 为独立页面，签署流程和保存页位于分包，原生自定义 Tab 选中状态为 0/1/2/3。
- [x] “签名”使用独立签名库图标，与中央“签署”图标不同。
- [x] 调整页长按文档位置可选择手写签名或已有签名，新签名落在长按位置并受文档边界约束。
- [x] 完整备份加密封装元数据和实际文件；备份正文不暴露签名名称、用户 ID 或文件内容。
- [x] PBKDF2-HMAC-SHA256 100,000 次派生密钥，AES-256-CBC 加密，HMAC-SHA256 校验；错误密码或密文篡改时拒绝恢复。
- [x] 密码为可选参数；留空时仍以应用保护模式加密且可直接恢复，填写时使用用户密码保护。
- [x] 模拟整个小程序沙箱清空后，输入正确密码可从外部 `.signmaster` 文件恢复。
- [x] 设置页提供备份生成、微信文件分享和微信文件导入恢复入口。
- [x] 生产 AppID 从 `VITE_MP_WEIXIN_APPID` 写入，`dist/build/mp-weixin` 可直接导入微信开发者工具。
- [x] 正式 `.env` 文件不进入 Git，仅提交 `.env.example`；源码清单不固化实际 AppID。
- [x] 微信构建临时注入 AppID，并在构建成功或失败后自动还原 `src/manifest.json`。
- [x] 扫描多页 PDF 生成器内联于扫描页面，iOS 不再跨模块调用扫描 PDF 导出函数。
- [x] 文档边沿结果过窄、面积过小或长宽比异常时回退完整照片。
- [x] 帮助与反馈提供操作指引、联系邮箱和公众号二维码，不伪装后端反馈提交。
- [x] 隐私说明与实际本地处理、权限、保留、备份、分享、删除和鉴别能力一致。
- [x] 关于页使用与小程序一致的真实 Logo，帮助页底部内容可完整滚动查看。
- [x] 小程序 Logo 保留原设计元素，外形适配为方形圆角，并提供 SVG 源文件和 512px PNG。
- [x] “我的”和“关于”页面展示 `闽ICP备2026014225号-3X` 及工信部备案查询入口。
- [x] CI 包含依赖审计、文案合规、敏感能力、类型、单元测试、微信生产构建、H5 E2E、CodeQL 和 Dependabot。

## 自动化命令

```bash
npm run typecheck
npm test -- --run
npm run build:h5
npm run test:e2e
npm run build:prod:mp-weixin
npm run test:mp-weixin
```

## 真机验收

- [ ] iPhone 刘海/灵动岛和微信胶囊不遮挡标题、数量、签字区域及按钮。
- [ ] 微信聊天文件选择后直接返回无 Loading；分别实选 PDF、PNG、JPG、JPEG、WEBP、BMP。
- [ ] 白纸、浅色桌面、深色桌面和低光照下连续扫描、裁边及多页 PDF 正常。
- [ ] 最粗笔连续书写 30 秒无虚线、累积延迟或触摸失效；橡皮连续擦除后仍可撤销重做。
- [ ] PDF、PNG、JPEG 导出可被微信打开，文件名只有一个句点且带正确日期时间。
- [ ] 三种格式切换后直接分享，微信好友收到的是当前选择格式。
- [ ] 重启小程序后模板、签名、默认签名、历史文件和本机用户 ID 仍存在。
- [ ] 真机分别生成无密码和有密码备份并发送到微信聊天，清除数据后验证直接恢复及密码恢复两条流程。

自动化结果：24 个单元测试文件、101 个用例；H5 E2E 原有流程 20/20，分享专项 1/1；文案合规、敏感能力、类型检查通过；微信生产构建与 dist 静态检查通过。扫描页编译产物已确认内联 PDF 生成且无旧运行时模块依赖，仍需按真机验收项复验连续扫描完成。
